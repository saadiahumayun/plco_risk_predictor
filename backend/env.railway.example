# Railway Environment Variables
# Add these to your Railway project settings (Settings > Variables)

# Environment
ENVIRONMENT=production
DEBUG=false

# Database (Railway will auto-populate if you add PostgreSQL)
# DATABASE_URL will be set automatically by Railway

# Security (generate a strong random key)
SECRET_KEY=your-super-secret-key-change-this-to-random-string

# CORS (your frontend URL)
BACKEND_CORS_ORIGINS=https://your-frontend.vercel.app,http://localhost:3000

# ML Model Settings
USE_MODEL_REGISTRY=false
ENABLE_MONITORING=true
LOG_PREDICTIONS=true

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=json

